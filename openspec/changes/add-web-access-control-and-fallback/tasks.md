## 1. 数据模型与配置

- [x] 1.1 为 `mcp_servers` 增加非空 `requires_network` 字段和 Alembic 迁移，按 HTTP/SSE=true、stdio=false 回填现有记录并验证升级/降级脚本
- [x] 1.2 扩展 MCP 创建、序列化与更新 Schema/API，使用户可查看和修改联网分类并实现按 transport 的缺省值
- [x] 1.3 增加并校验 LLM 请求超时、最大重试次数、退避基数及可选全站网络工具总开关配置
- [x] 1.4 增加聊天请求幂等标识的持久化字段与用户/会话范围唯一约束，定义重复请求的复用查询

## 2. 联网权限执行链

- [x] 2.1 给 `ChatRequest` 增加缺省为 false 的 `allow_network` 和可选 `client_request_id`，并贯穿 send/stream、`ChatService` 与 `ContextBuilder`
- [x] 2.2 扩展 MCP 路由元数据和 `openai_tools`，在联网关闭时过滤 `requires_network=true` 的服务工具
- [x] 2.3 扩展 `execute_tool` 接收本次联网许可，并在入队前二次拒绝未授权网络工具，返回 `network_access_denied`
- [x] 2.4 调整系统提示和上下文元数据，禁止在无成功网络工具结果时声称已联网，并在无可用网络工具时继续本地回答
- [x] 2.5 为联网关闭、开启、无网络工具、伪造网络工具调用和用户隔离增加后端单元/集成测试

## 3. 异常分类与后端兜底

- [x] 3.1 建立内部错误码、可重试性和安全用户消息映射，覆盖 LLM、MCP、记忆、文档检索、工具循环与 SSE 阶段
- [x] 3.2 在 LLM 客户端实现仅限首 token 前瞬时故障的超时与有界指数退避重试，并排除认证、权限和参数错误
- [x] 3.3 让记忆与个人文档检索分别隔离失败并收集 degradation code，避免任一可选上下文依赖阻断回答
- [x] 3.4 标准化 MCP 失败工具结果；网络工具失败后允许模型本地收尾，并要求明确披露未取得在线结果
- [x] 3.5 在工具轮次超限时执行一次禁用工具的最终综合调用，并为最终调用失败接入统一终止兜底
- [x] 3.6 在流式异常中保存部分内容为 `interrupted`，禁止自动重放已输出流，并确保无内容失败不会生成误导性完整消息
- [x] 3.7 实现 `client_request_id` 幂等重试，确保重复提交不新增用户消息且并发冲突可安全复用

## 4. SSE 契约与前端体验

- [x] 4.1 扩展 SSE `done` 事件返回 `degraded`/`degradations`，扩展 `error` 事件返回 code、message、recoverable、request_id 和 partial_saved，同时保留兼容字段
- [x] 4.2 在聊天输入区实现默认关闭的联网开关、准确帮助文案和本地偏好记忆，并把发送时状态传入聊天 store/API
- [x] 4.3 在前端消息状态中保存每次请求的联网许可和客户端请求 ID，确保重试精确重放原始选项
- [x] 4.4 在消息气泡显示非阻塞降级提示，在终止错误中显示安全信息，并只对 recoverable 错误提供重试按钮
- [x] 4.5 扩展 MCP 设置表单和列表，展示/编辑 `requires_network` 并提示用户复核 stdio 工具的真实网络行为

## 5. 可观测性与安全

- [x] 5.1 为聊天请求生成 request ID，输出包含阶段、错误码、尝试次数、降级项和耗时的结构化日志
- [x] 5.2 增加请求成功率、降级率、错误码、重试成功率与网络工具耗时指标，并确认标签不包含高基数字段
- [x] 5.3 增加日志脱敏测试，确认密钥、Cookie、MCP Header、完整用户消息、工具参数、内部 URL 和上游响应体不进入日志或 SSE

## 6. 验证与发布

- [x] 6.1 增加 LLM 瞬时恢复、重试耗尽、不可重试 4xx、首 token 后中断和工具轮次超限的后端测试
- [x] 6.2 增加 SSE 完成、降级、结构化错误、断流保留及旧客户端缺省字段兼容测试
- [x] 6.3 增加前端联网开关、偏好恢复、降级提示、可恢复重试和防重复发送测试
- [x] 6.4 运行后端测试、前端类型检查/构建和 OpenSpec 校验，并记录部署顺序、观测阈值与回滚步骤
